export default function CheckBox() {
  return (
    <input
      //   className={style.checkbox}
      type="checkbox"
      name=""
      id=""
      //   onClick={() => {
      //     onClick(true);
      //   }}
    />
  );
}
